Question: Add a short written note (comment or README) explaining the difference between the core Playwright package and the Playwright test runner package, 

Based on My understanding.
Playwright package: Requires explicit setup and teardown of browser, context, and page instances

from playwright.sync_api import Playwright

def test_login(playwright:Playwright):
    browser=playwright.chromium.launch()
    context = browser.new_context()
    page=context.new_page()

Playwright test runner package : 
Uses built-in Pytest fixtures (e.g., `page`, `context`, `browser`).
   - Automatically manages browser lifecycle (launch, context creation, and cleanup) in the background
   
from playwright.sync_api import Page
def test_login(page:Page):
    page.goto('url')
